export interface VariantProps {
  primary?: boolean;
  secondary?: boolean;
  info?: boolean;
  warning?: boolean;
  danger?: boolean;
  success?: boolean;
}

export interface VariantStyles {
  primary?: object;
  secondary?: object;
  info?: object;
  warning?: object;
  danger?: object;
  success?: object;
}
