import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Alert from '../../src/Alert';
import * as stories from './Alert.stories.tsx';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import { argTypes } from './args';

<Meta title="Components/Alert" component={Alert} argTypes={argTypes} />

# Alert

Alert banners notify users of important information or changes on a page. Typically, they appear at the top of a page. The alert can either be passed
a string to display through the "content" prop, or be passed children to render its content.

export const Template = args => <Alert {...args} />;

<Canvas>
  <Story story={stories.Default} name="Default" />
</Canvas>

## Arguments

<ArgsTable story="Default" />

## Variants

<Canvas>
  <Story story={stories.Info} name="Info" />
  <Story story={stories.Warning} name="Warning" />
  <Story story={stories.Danger} name="Danger" />
</Canvas>

## HTML only

<Canvas>
  <Story story={stories.HTML} name="html" />
</Canvas>
