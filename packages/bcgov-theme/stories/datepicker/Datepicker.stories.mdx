import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import DatePicker from '../../src/DatePicker';
import * as stories from './Datepicker.stories.tsx';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import { argTypes } from './args';

<Meta title="Components/DatePicker" component={DatePicker} argTypes={argTypes} />

# Date Picker

The date picker allows users to select a date.

export const Template = (args) => <DatePicker {...args} />

<Canvas>
  <Story story={stories.Default} name="Default" />
</Canvas>

## Arguments

<ArgsTable story="Default" />

## HTML only

<Canvas>
  <Story story={stories.HTML} name="html" />
</Canvas>
