import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Dropdown from '../../src/Dropdown';
import * as stories from './Dropdown.stories.tsx';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import { argTypes } from './args';

<Meta title="Components/Dropdown" component={Dropdown} argTypes={argTypes} />

# Date Picker

Dropdowns allow users to select one option from a list. Pass '<option\>'s as children to the component.

export const Template = (args) => <Dropdown {...args} />

<Canvas>
  <Story story={stories.Default} name="Default" />
</Canvas>

## Arguments

<ArgsTable story="Default" />

## HTML only

<Canvas>
  <Story story={stories.HTML} name="html" />
</Canvas>
