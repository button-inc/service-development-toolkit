import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Textarea from '../../src/Textarea';
import * as stories from './TextArea.stories.tsx';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import { argTypes } from './args';

<Meta title="Components/Textarea" component={Textarea} argTypes={argTypes} />

# Textarea

A textarea allows users to input multiple lines of text.

export const Template = (args) => <Textarea {...args} />

<Canvas>
  <Story story={stories.Default} name="Default" />
</Canvas>

## Arguments

<ArgsTable story="Default" />

## HTML only

<Canvas>
  <Story story={stories.HTML} name="html" />
</Canvas>
